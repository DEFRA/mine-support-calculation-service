version: '3'
services:
  mine-support-calculation-service:
    environment:
      MINE_SUPPORT_MESSAGE_QUEUE: amqp://mine-support-message-queue
    networks:
      - mine-support

networks:
  mine-support:
    external: true
